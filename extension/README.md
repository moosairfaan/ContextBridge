# ContextBridge (Chrome extension)

Legacy MV3 browser extension that lives next to the web app in this monorepo.

## Develop

```bash
cd extension
npm install
npm run build
```

Load unpacked from `extension/dist` in `chrome://extensions`.

The primary product is the web app at the **repo root** (paste UI + bookmarklet). See the root `README.md`.
